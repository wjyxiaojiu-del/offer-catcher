DATABASE_URL="file:./prisma/dev.db"

# Required in production. Users must authenticate with this value before
# accessing resume, application, AI, or browser automation APIs.
OFFER_CATCHER_ACCESS_TOKEN="replace-with-a-long-random-token"

# Optional LLM provider configuration.
MIMO_API_KEY=""
MIMO_BASE_URL="https://token-plan-sgp.xiaomimimo.com/v1"
MIMO_MODEL="mimo-v2.5-pro"
MIMO_TIMEOUT_MS="30000"

# Dangerous browser automation is disabled unless explicitly enabled.
BOSS_AUTOMATION_ENABLED="false"
BOSS_HEADLESS="true"

# Optional: disable LLM planner for deterministic fallback behavior.
DISABLE_LLM_PLANNER="false"

# Optional: MinerU OCR service for scanned PDFs.
# Deploy MinerU: https://github.com/opendatalab/MinerU
# When configured, scanned/image PDFs will be automatically processed via OCR.
MINERU_API_URL=""
MINERU_API_KEY=""
